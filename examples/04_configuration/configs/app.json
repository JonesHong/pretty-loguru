{
  "app_name": "MyApplication",
  "version": "1.0.0",
  "logging": {
    "default": {
      "log_dir": "./logs/app",
      "preset": "daily"
    },
    "modules": {
      "auth": {
        "log_dir": "./logs/app/auth",
        "preset": "detailed",
        "retention": "7 days"
      },
      "api": {
        "log_dir": "./logs/app/api",
        "preset": "hourly",
        "retention": "3 days"
      },
      "database": {
        "log_dir": "./logs/app/db",
        "preset": "simple",
        "retention": "14 days"
      }
    }
  }
}